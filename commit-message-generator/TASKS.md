# Tasks

## Done

- [2026-08-02] Fixed Windows cp1252 encoding crash on non-ASCII diffs/messages —
  forced UTF-8 at all subprocess/IO boundaries in `generate_commit_message.py`.
  See CHANGELOG.md (full analysis in git history).
